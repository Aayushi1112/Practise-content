package com.policy.creation.MongoExample.repository;

import java.util.List;

import org.springframework.data.domain.Sort;
import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

import com.policy.creation.MongoExample.controller.Limit;
import com.policy.creation.MongoExample.model.Student;
@Repository
public interface StudentRepository extends MongoRepository<Student,Integer> {

	
	//List<Student> findByLastname(String lastname, Sort sort, Limit limit);
}
